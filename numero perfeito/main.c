#include <stdio.h>
#include <stdlib.h>

int main()
{
    int qtd, numero;
    scanf("%d", &qtd);
    for(i=0; i<qtd; i++)
    {
        scanf("%d", &numero);
        n_perf=numero
    }

    return 0;
}
